server:
  servlet:
    context-path: /api

spring:
  application:
    name: snaptrade

  datasource:
    url: jdbc:mysql://localhost:3306/snap_trade?serverTimezone=Asia/Seoul
    username: root
    password: root

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: create